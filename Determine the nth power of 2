#include <stdio.h>

int is_num(int n)
{
	if ((n & (n - 1)) == 0)
	{
		return 1;
	}
	return 0;
}

int main()
{
	int n = 0;
	scanf("%d", &n);
	if (is_num(n))
	{
		printf("该数字是2的n次方数。");
	}
	return 0;
}
